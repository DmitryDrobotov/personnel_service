<% title "Departments" %>
<ul>
  <% for department in @departments %>
    <li>
      <%= link_to department.name, department_path(department) %>
    </li>
  <% end %>
</ul>

<div>
  <%= button_to "New Department", new_department_path, :class => "btn primary", :method => :get %>
</div>