<% title "Department" %>

<p>
  <strong>Name:</strong>
  <%= @department.name %>
</p>

<p>
  <%= link_to "Edit", edit_department_path(@department) if can? :update, @department %> |
  <%= link_to "Destroy", @department, :confirm => 'Are you sure?', :method => :delete if can? :destroy, @department %> |
  <%= link_to "View All", departments_path if can? :read, Deaprtment %>
</p>
